/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/* 
 * File:   ListEnemies.h
 * Author: Simo
 *
 * Created on 29 giugno 2017, 0.04
 */

#ifndef LISTENEMIES_H
#define LISTENEMIES_H

//(used for print the list)
#include <iostream>
//#include "Map.h"

/*
using namespace std;

struct listEnemy {
  human enemy;
  int floor;
  listEnemy* pred;
  listEnemy* succ;
};

typedef listEnemy* ptrEnemy;

struct Node {
    human enemy;
    int floor;
    Node* next;
    Node* prev;
};

typedef Node* ptrN;


// ATTENZIONE: il primo elemento e' vuoto
// per semplificare il primo elemento deve avere livello (-1)


ptrN initializeList(ptrN head);

ptrN insert(ptrN h, human human, int floor);

bool empty(ptrN h);

ptrN next(ptrN p);

ptrN prev(ptrN p);

human read(ptrN p);

void write(ptrN p,human human, int v);

ptrN remove(ptrN p);

//se non ci sono nemici del livello giusto va in loop
ptrN firstEnemy(ptrN enemy, int liv);

ptrN lastEnemy(ptrN enemy, int liv);

int enemiesLeft(ptrN h, int level);

void print(ptrN h);
*/
#endif /* LISTENEMIES_H */

